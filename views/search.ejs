<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Search</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <div class="container">
        <div class="form-container">
            <form action = "/search" method="POST">
                <input type="text" name="movieTitle" placeholder="Enter a Movie Title">
                <button type="submit">Search</button>
            </form>
        </div>

    <% if(movieDetails) { %> <!--kui ei ole midagi sisestatud, siis ei näita neid andmete pealkirju e kui movieDetails ei ole tühi objekt, näitab edaspidiseid andmeid.-->
        
        <div class="result-container">
            <ul class="movie-data-list">
                <li><span>Title: </span><%= movieDetails.title %></li>
                <li><span>Year: </span><%= movieDetails.year %></li>
                <li><span>Genres: </span><%= movieDetails.genres %></li>
                <li><span>Overview: </span><%= movieDetails.overview %></li>
            </ul>

            <div class="image-container">
                <img src="<%= movieDetails.posterUrl %>" alt="">
            </div>

        </div>

    <% } %>

    </div>
</body>
</html>